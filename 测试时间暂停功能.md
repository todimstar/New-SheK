# 测试时间暂停功能指南

本指南将帮助您在Unity场景中测试时间暂停功能，通过添加可视化元素来展示Time.timeScale的效果。

## 准备工作

1. 确保您的场景中已包含UI系统和PanelManager
2. 确保PlayStopUI脚本中的时间暂停功能已实现

## 步骤一：添加测试物体

### 创建受时间影响的旋转立方体

1. 在Hierarchy面板中，右键单击 -> 选择 "3D Object" -> "Cube"
2. 将新创建的立方体命名为 "TimeScaledCube"
3. 将立方体放置在摄像机的可视范围内，例如位置设为 (0, 0, 10)
4. 为了使立方体更明显，可以调整其大小为 (2, 2, 2)
5. 在Inspector面板中，点击 "Add Component" -> 搜索并添加 "RotatingCube" 脚本
6. 确保 "Use Time Scale" 选项被勾选（默认勾选）

### 创建不受时间影响的移动球体（可选，用于对比）

1. 在Hierarchy面板中，右键单击 -> 选择 "3D Object" -> "Sphere"
2. 将新创建的球体命名为 "UnscaledSphere"
3. 将球体放置在摄像机的可视范围内，例如位置设为 (0, 3, 10)
4. 为了区分，可以更改球体的材质颜色（创建新材质并赋予不同颜色）
5. 在Inspector面板中，点击 "Add Component" -> 搜索并添加 "UnscaledMovement" 脚本

### 添加时间缩放显示文本（可选）

1. 在Hierarchy面板中，右键单击 -> 选择 "UI" -> "Text - TextMeshPro"
2. 将文本命名为 "TimeScaleText"
3. 设置文本内容为 "Time Scale: 1.00"
4. 调整文本位置，确保在屏幕上清晰可见
5. 在 "TimeScaledCube" 的 "RotatingCube" 组件中，将 "Time Scale Text" 字段设置为此文本

### 添加全局按键监听器（推荐）

1. 在Hierarchy面板中，右键单击 -> 选择 "Create Empty"
2. 将新创建的空物体命名为 "KeyboardManager"
3. 在Inspector面板中，点击 "Add Component" -> 搜索并添加 "KeyboardManager" 脚本
4. 该脚本将确保无论在哪个界面，按ESC键都能正常切换暂停状态

## 步骤二：测试时间暂停功能

现在，您可以通过以下方式测试时间暂停功能：

1. 运行场景
2. 观察立方体的旋转动画和球体的移动动画（如果已添加）
3. 按下 "ESC" 键打开暂停面板
4. 观察立方体的旋转是否停止，而球体继续移动
5. 注意查看 "Time Scale" 文本值是否变为 "0.00"
6. 再次按下 "ESC" 键或点击关闭按钮，恢复游戏
7. 观察立方体的旋转是否恢复，"Time Scale" 文本值是否恢复为 "1.00"

## 其他测试方法

除了按ESC键外，您还可以：

1. 通过点击PlayerHUDUI上的菜单按钮打开暂停面板
2. 观察是否也能正确暂停游戏时间

## 时间暂停原理说明

1. **Time.timeScale**:
   - Unity中控制时间流逝速度的变量
   - 值为1.0表示正常速度
   - 值为0表示时间完全暂停
   - 值大于1表示时间加速
   - 值在0到1之间表示时间减速

2. **受时间影响的函数/变量**:
   - Time.deltaTime: 受Time.timeScale影响
   - 基于Update/FixedUpdate的动画和物理模拟
   - Animation组件的动画

3. **不受时间影响的函数/变量**:
   - Time.unscaledDeltaTime: 不受Time.timeScale影响
   - Time.unscaledTime: 不受Time.timeScale影响
   - LateUpdate中的一些操作
   - 基于协程的定时器(需特殊处理)

## 注意事项

1. 当Time.timeScale设为0时，基于deltaTime的所有操作都会停止
2. UI交互不受Time.timeScale影响，所以暂停菜单在Time.timeScale为0时仍然可以操作
3. 如果需要在游戏暂停时实现某些动效，应使用Time.unscaledDeltaTime
4. 某些第三方插件可能对Time.timeScale的响应方式不同，需单独测试 

## 疑难问题解答

### Q: 按ESC键没有反应？
A: 可能是因为多个脚本同时监听了ESC键，导致冲突。使用KeyboardManager作为统一的按键处理器可以解决这个问题。

### Q: 暂停后UI无法交互？
A: 确保UI的Canvas在"Screen Space - Overlay"模式下，这种模式下的UI不受Time.timeScale影响。

### Q: 某些动画在暂停时仍在播放？
A: 检查这些动画是否使用了Time.unscaledDeltaTime或自定义的时间系统。对于Mecanim动画，可能需要调整动画器的更新模式。 